package com.galya.business.productioncapacity.components;

import java.awt.GridLayout;
import java.awt.Dialog.ModalityType;

import javax.swing.ImageIcon;
import javax.swing.JDialog;
import javax.swing.JLabel;

public class LoadingDataDialog {

/*    private static final int DIALOG_WIDTH = 400;
    private static final int DIALOG_HEIGHT = 250;

    private JDialog savingIsLoadingDialog;

    public LoadingDataDialog() {
        savingIsLoadingDialog = new JDialog();
        savingIsLoadingDialog.setLayout(new GridLayout(1, 1));
        savingIsLoadingDialog.setSize(DIALOG_WIDTH, DIALOG_HEIGHT);

        savingIsLoadingDialog.setDefaultCloseOperation(JDialog.DO_NOTHING_ON_CLOSE);
        savingIsLoadingDialog.setAutoRequestFocus(true);
        savingIsLoadingDialog.setResizable(false);
        savingIsLoadingDialog.setModalityType(ModalityType.DOCUMENT_MODAL);

        ImageIcon imageIcon = new ImageIcon("resources" + System.getProperty("file.separator") + "animated.gif");
        JLabel picLabel = new JLabel(imageIcon);
        savingIsLoadingDialog.add(picLabel);
    }

    public void setVisible() {
        savingIsLoadingDialog.setVisible(true);
    }
    
    public void setInvisible() {
        savingIsLoadingDialog.setVisible(false);
    }*/
}
